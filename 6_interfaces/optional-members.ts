// Optional variable using the `?`
namespace InterfaceOptionalMembers{

    interface IOptionalMembers{
        nonOptionalVariable: boolean;
        optionalVariable?: boolean; // `?` means that this property is optional
    }
}